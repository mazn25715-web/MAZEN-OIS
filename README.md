# Mazen OS

Debian 13 (Trixie) + XFCE foundation for a real bootable Mazen OS ISO.

## Build
`sudo apt install -y live-build debootstrap squashfs-tools xorriso git`
then `chmod +x build.sh && ./build.sh`

Output: `Mazen-OS.iso`
